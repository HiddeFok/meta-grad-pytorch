name: tests

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-export:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Install PyTorch (CPU only)
      run: pip install torch --index-url https://download.pytorch.org/whl/cpu
    - name: install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install .
    - name: Test with pytest
      run: |
        python -m pytest --cov=./metagrad --cov-report xml
    - name: Creating coverage folder
      run: |
        mkdir -p coverage
    - name: Coverage Badge
      run: |
        genbadge coverage -n "pytest" -i ./coverage.xml -o ./coverage/coverage_badge.svg
    - name: Export Coverage Badge 
      uses: exuanbo/actions-deploy-gist@v1
      with:
        token: ${{ secrets.TOKEN }}
        gist_id: be967539e481ae6bb8bb785c4cac289e
        file_path: ./coverage/coverage_badge.svg
        file_type: text
    
      
    
